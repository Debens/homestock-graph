input NewContainer {
    name: String!
}

type Query {
    containers: [Container]
    container(id: String, name: String): Container
}

type Mutation {
    createContainer(container: NewContainer): Container
}
